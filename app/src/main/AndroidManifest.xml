<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ulusoyapps.navigations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".main.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".main.ActivityA" />
        <activity android:name=".main.ActivityB" />
        <activity android:name=".main.ActivityC" />
        <activity android:name=".main.ActivityD" />
        <activity android:name=".main.ActivityE" />
        <activity android:name=".main.ActivityF" />

        <meta-data android:name="EasyGoClient" android:value="true" />

    </application>


</manifest>